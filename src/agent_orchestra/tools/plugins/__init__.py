"""Drop-in tool plugins.

Any module placed here that defines a concrete ``Tool`` subclass is discovered
and registered automatically at startup — no edits to the harness core required.
See ``find_in_files.py`` for a worked example.
"""
